# The daemon and both fleet nodes run the Node in .nvmrc (22). better-sqlite3
# compiles a native addon against that ABI, so installing under a different
# Node rebuilds it for the wrong NODE_MODULE_VERSION and the daemon dies on
# its first query. Refuse the install instead of shipping a broken addon.
engine-strict=true
